# P0 negative fixtures

Generated invalid MP4s live only under the ignored `out/p0/.../fixtures/` directory.
This tracked fixture directory intentionally contains no copied production assets: P0 must never mutate
the normal baseline or source assets while proving invalid artifact handling.
