# p1-XX · <一行で何をするか>

> 雛形。新しい packet はこれを複製して埋める。空欄を残したまま実装セッションへ渡さない。

**カード:** `MX-YY`（該当が無ければ「なし」と書く。空欄にしない）
**branch:** `codex/p1-XX`。`main` から分岐する。**積まない**
**担当:** Forge（実装セッション）
**読むもの:** [README](README.md) の実行規約 → `AGENTS.md` → 本 packet

## 目的

このカードが**何を証拠として残すか**を1〜2文で。作業内容ではなく成果の定義を書く。

## 承認済み範囲

- 何が承認されているか（`M3-03` / `M3-01` / ADR-001 のどれに由来するか）
- **承認されていないもの**を明示する。書いていないものは承認されていない
- budget は `pipeline/m3/p1-track-a-run-definition.json` の該当エントリを**参照**する。数値を書き写さない

## 依存

| 種別 | 内容 |
| --- | --- |
| 先行 packet | |
| merge 済みが必要な PR | |
| owner 判断 | |

依存が満たされていなければ**着手しない**。満たされているかの確認方法も書く。

## 実装対象

**触ってよいディレクトリはここだけ。**

```
pipeline/m3/p1/<dir>/
```

## 触ってはいけないもの

- `docs/character-expression-program-kanban.md` — Orbit が merge 波ごとに更新する
- `docs/anti-patterns.md` — AP番号は Orbit が払い出す。観測したら要求する
- `package.json` — P1 script は `p1-i1` がまとめて追加済み
- 他 packet のディレクトリ
- （packet 固有のものがあれば追記）

## 実装内容

何を作るか。ファイル単位で。既存資産の再利用先を明記し、再実装させない。

## 受入証拠

実行するコマンドと、通らなければならない検査。

| コマンド | 期待 |
| --- | --- |
| `npm run typecheck` | pass |
| `npm test` | pass |
| | |

**否定テストを必ず含める。** 「正しい入力で通る」だけのテストは、一様に間違った実装を通す。
何が**落ちなければならないか**を列挙する。落ちる fixture が無い PR はテストされていない。

## STOP 条件

該当したら止めて報告する。吸収しない。

- budget 超過（revision / render attempt）
- 新規 dependency、add-on、cloud / 有料サービスが必要になった
- firewall rule その他ホスト設定の変更が必要になった
- （packet 固有）

## 非対象

- 品質順位付け、方式採択、`representationDecision` の記入
- render の許可を与えること
- （packet 固有）

## PR 本文に書くこと

- カードの状態遷移（盤面は触らない）
- 受入証拠の実行結果
- 観測した failure と、それが一般化するなら AP番号の払い出し要求
- **判断を要する事項があれば明示する。既定値で流さない**
