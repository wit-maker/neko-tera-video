# Core

- Current production video is delivered; resume state and verified metrics: `PROJECT_STATE.md`.
- Next character-representation initiative starts from `docs/character-architecture-strategy.md`; it is a proposal, not an adopted architecture.
- Reusable failures are permanent engineering assets. Read/update policy and active entries: `mem:anti_patterns`; repository SSOT: `docs/anti-patterns.md`.
- Current local mouth-patch renderer is a frozen comparison baseline, not a constraint on next-generation assets.
- Architecture comparisons share character bible, semantic motion intent, camera, output, and evaluation; they do not force one PNG/asset structure across adapters.
- Detailed stack: `mem:tech_stack`. Commands: `mem:suggested_commands`. Project conventions: `mem:conventions`. Completion gates: `mem:task_completion`.