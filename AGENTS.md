# neko-tera-video エージェント規約

このファイルは、役割や実行環境が変わっても引き継ぐプロジェクト固有の規約である。

## 着手前の必読

1. 現在地は `PROJECT_STATE.md` で確認する。
2. LLM共通の設計、比較、証拠、権限モデルは `LLM_GUIDE.md` で確認する。
3. 判断状態と規約への昇格条件は `docs/architecture-governance.md` で確認する。
4. キャラクター表現基盤の提案は `docs/character-architecture-strategy.md` を起点にする。
5. ツール追加の提案は `docs/tooling-extension-strategy.md` を起点にする。
6. 実装、調査、PoCの前に `docs/anti-patterns.md` の該当項目を確認する。
7. 旧動画の詳細な判断根拠と実測値が必要なときだけ `docs/cto-handoff.md` を参照する。

`LLM_GUIDE.md`はCodex、Claude、そのほかのLLMが共有する正本である。

共通規則をprovider固有のmemoryや入口ファイルへ複製しない。

## 失敗を永続資産にする

- 失敗、誤った仮定、無効な比較、偽陽性、偽陰性、再現不能、成果物破損から、別タスクでも再利用できる知見が得られた場合、タスク完了前に `docs/anti-patterns.md` へ記録する。
- 記録は人やエージェントを責めず、**観測事実、証拠、根本原因、禁止するパターン、代替策、適用範囲**を残す。
- 一過性のタイプミスや、再利用可能な教訓のない失敗は記録しない。秘密情報、認証情報、raw prompt は記録しない。
- 台帳への追記だけで終わらせない。再発を機械的に防げる場合は、同じ変更でテスト、validator、受入条件、テンプレート、またはADRへ昇格する。
- 過去の記録は削除しない。前提が変わった場合は `superseded` と後継IDを追記する。

## 承認状態を混ぜない

- mi3sanの明示指示、レビュー上のguardrail、設計提案、未決事項、実験結果、採択済み決定を区別する。状態の定義は `docs/architecture-governance.md` に従う。
- `PROPOSED` または `OPEN` の内容は、実装要件、不変条件、採択済みアーキテクチャとして扱わない。
- 設計提案を恒久規約へ昇格するには、mi3sanの承認とADRまたは同等の決定記録を必要とする。
- ラピスのレビュー指摘は、採否が確定するまで `REVIEW_GUARDRAIL` または争点として保持する。アトラス単独で承認済みへ変更しない。
- 観測済み失敗と将来リスクを混ぜない。前者は証拠付きでアンチパターン台帳へ、後者は戦略文書またはリスク台帳へ置く。

## オーナー指示とレビューguardrail

- 表現方式を先に選び、その方式が本来必要とする素材を制作する。同じ完成PNGを全方式へ強制しない。
- mi3sanの最終判断は、各方式から得た実映像を通常速度、スロー、拡大、デバッグ表示などで比較して行う。
- 音素認識や音声同期は描画方式の比較から分離する。
- 視覚品質、制作コスト、計算資源、再現性、操作性、ライセンスは分けて提示し、未合意の重み付き総合点へ潰さない。
- 推測を合格根拠にせず、最終表示系の成果物と実測値を証拠にする。

## 次期キャラクター比較の採択済み境界

正本は `docs/adr/001-next-character-comparison-boundaries.md` とする。

- P1の暫定視点範囲はyaw ±15°、pitch ±10°。
- 最初の候補選定は事前レンダーだけで行い、リアルタイム性能は後段で別評価する。
- P2では現行キャラクターの同一性、輪郭、毛並み、色面、口腔表現を最低限維持し、写実化を要求しない。
- RTX 3060 Ti 8GBによるローカル実行を優先する。
- クラウドまたは有料サービスは、mi3sanが自分から明示した場合、または用途、費用、送信データ、代替経路を提示した提案へmi3sanが明示承認した場合だけ利用する。
- 沈黙や過去承認を流用しない。
