# neko-tera-video エージェント規約

このファイルは、役割や実行環境が変わっても引き継ぐプロジェクト固有の規約である。

## 着手前の必読

1. 現在地は `PROJECT_STATE.md` で確認する。
2. キャラクター表現基盤の判断は `docs/character-architecture-strategy.md` を起点にする。
3. 実装・調査・PoCの前に `docs/anti-patterns.md` の該当項目を確認する。
4. 旧動画の詳細な判断根拠と実測値が必要なときだけ `docs/cto-handoff.md` を参照する。

## 失敗を永続資産にする

- 失敗、誤った仮定、無効な比較、偽陽性・偽陰性、再現不能、成果物破損から、別タスクでも再利用できる知見が得られた場合、タスク完了前に `docs/anti-patterns.md` へ記録する。
- 記録は人やエージェントを責めず、**観測事実、証拠、根本原因、禁止するパターン、代替策、適用範囲**を残す。
- 一過性のタイプミスや、再利用可能な教訓のない失敗は記録しない。秘密情報、認証情報、raw prompt は記録しない。
- 台帳への追記だけで終わらせない。再発を機械的に防げる場合は、同じ変更でテスト、validator、受入条件、テンプレート、またはADRへ昇格する。
- 過去の記録は削除しない。前提が変わった場合は `superseded` と後継IDを追記する。

## 比較実験の不変条件

- 同じPNGを各方式へ強制しない。共通化するのはキャラクターバイブル、顔動作意図、カメラ、尺、出力仕様、評価手順である。
- 方式ごとに、その方式が本来必要とする素材を用意する。
- 視覚品質、制作コスト、計算資源、再現性、操作性、ライセンスを別々に評価し、未合意の重み付き総合点へ早期に潰さない。
- Remotionは比較・注釈・提示の層とし、方式固有の変形やシミュレーションの正解実装にはしない。
- 推測を合格根拠にしない。最終表示系の成果物と実測値を証拠にする。

